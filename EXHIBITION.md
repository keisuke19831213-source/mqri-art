# 展示モード運用ガイド — MQRI ART

投影用の自動巡回ページ。**クリック1回で開演 → あとは放置で作品を循環**。

- 本番URL：**https://art.mqri.or.jp/exhibit/**
- 旧URL（`ccf5-gallery/kiosk.html`）はここへ誘導される。QRの印刷物が生きているので旧URLは殺さないこと
- 来場者に配る用の作品集：**https://art.mqri.or.jp/**

## オフライン運用（会場Wi-Fiが不安定なとき）

`file://` で開くと iframe が別オリジン扱いになり、**作品固有UIの自動非表示とキー操作の中継が効かなくなる**（`contentDocument` に触れないため）。ローカルでは必ずサーバ経由で開く。

```sh
cd ~/Documents/Products/mqri-art && npx serve .
# → http://localhost:3000/exhibit/
```

vault の `.claude/launch.json` にも `mqri-art`（ポート4890）を用意してある。最新化は `git pull`。

## 上映順（無から満ちるへ・受付〜開演の弧）

1. 幾何の創世（無から最初の円へ＝開場・人がまばらな時間に）
2. 無限幾何回廊（静かに潜行・催眠的）
3. 共鳴譜（砂が音を見る・無音運用）
4. HOPF LOOM（虹色の多次元）
5. 量子曼荼羅（生命の花＝満ちる・開演へ）

→ 1作品**90秒**・5作品7.5分＝30分で**約4周**。`exhibit/index.html` の `DWELL_SEC` で調整（ゆっくり=120〜150／速く=60）。作品固有の操作説明・タイトルは自動で非表示、左下の壁ラベルのみ表示（当日は `L` キーでON/OFF）。

**番組を伸ばすには** `exhibit/index.html` の `ORDER` に `{"slug":"...","no":"...","ttl":"...","en":"...","cap":"..."}` を足す。`slug` は `works/<slug>/` のフォルダ名。現在 `works/` には18点ある。

> ⚠ 作品を `ORDER` に足すときは `HIDE` にも対応する行を足すこと（作品ごとに固有UIのセレクタが違う）。**セレクタは必ずその作品の `index.html` を見て確認する** — 一括指定は canvas を誤爆する危険があるうえ、同じ作品名でも実装が違うことがある（`hopf-loom` は iPad展示版なので、旧 ccf5-gallery 時代の `#hud,.jp,.keys` ではなく `#topbar,#controls`）。

## 当日の起動手順（開演の40分前を目安に）

1. Chromeで `https://art.mqri.or.jp/exhibit/` を開く（オフライン運用なら上記のローカルサーバ）
2. 中央の「**▶ クリックで開演（全画面）**」を1回クリック → 自動で全画面＆巡回開始
3. あとは放置。作品は90秒ごとにフェードで切り替わる

- 操作キー：`→`次 ／ `←`戻る ／ `Space`一時停止 ／ `F`全画面 ／ `L`ラベルON/OFF ／ `C`時計表示 ／ `Esc`終了
- **作品をクリックして触った後もキー操作は効く**（iframe内にもキー監視を差し込んである）

## 事前準備（前日までに）

- [ ] **実機のプロジェクタで一度テスト**（GPUによってWebGLの重さが違う。特に量子曼荼羅は負荷が高い）
- [ ] オフライン版が開けるか確認（会場Wi-Fiが不安定でも動くように）
- [ ] ノートPCの設定：
  - [ ] **電源アダプタを接続**（バッテリー切れ厳禁）
  - [ ] **スリープ／スクリーンセーバをOFF**（システム設定 > ロック画面 → すべて「しない」）
  - [ ] **通知OFF**（集中モード／おやすみ）
  - [ ] 画面の明るさMAX・Night ShiftはOFF
- [ ] 表示：プロジェクタは**ミラーリング**でOK。解像度は1920×1080（16:9）目安

## iPadでの常設展示

作品を1点だけ据え置くなら、`exhibit/` ではなく作品URLを直接使う。手順は各作品フォルダの README を参照（Safariで開く → ホーム画面に追加 → アクセスガイド → 自動ロック「なし」）。ホーム画面アプリ化すると Safari のUIと終了ボタンが消え、来場者が誤って作品から離脱できない「壊されない展示」になる。

## 音まわり

- アート側は**無音運用**（共鳴譜も、放置なら音は鳴らない）
- 受付BGMは**蜂ミュージック**を別で流すと、場の周波数と世界観が揃う

## トラブル時

- 画面が真っ黒／カクつく → そのプロジェクタのGPUが重い可能性。`→`で次へスキップ（事前テストで回避）
- 全画面にならない → `F`キー、またはChromeメニューから全画面
- 途中で止まった感じ → `Space`で一時停止を解除、それでもダメなら再読み込み→開演クリック
- **作品のタイトルや操作説明が消えない** → `HIDE` のセレクタがその作品の実装と合っていない。作品の `index.html` を見て直す
- 来場者が自分のスマホで見たい → https://art.mqri.or.jp/ のQRを受付に掲示
